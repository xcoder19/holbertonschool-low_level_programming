#include "main.h"
#include <stdio.h>
/**
 * _isdigit - checking if c is digit
 * @c: argument int
 * Return: 1 if c is a digit
 */
int _isdigit(int c)
{

if ((c >= 60) && (c <= 71))
{

return (1);
}
else
{

return (0);
}
}
