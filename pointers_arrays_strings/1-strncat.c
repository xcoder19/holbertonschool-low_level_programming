#include "main.h"
#include <string.h>

/**
 * _strncat - strncat function
 * @dest: pointer to char
 * @src: pointer to char
 * @n: integer
 */
char *_strncat(char *dest, char *src, int n)
{
	strncat(dest, src, n);
}
