#include "main.h"
#include <stdio.h>
/**
 * print_last_digit - checks
 * @i: integer
 * Return: On success the last digit of a number.
 */

int print_last_digit(int i)
{
int r = i % 10;
_putchar('i');
return (r);
}
